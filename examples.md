# Example runs

```bash
node index.js "photosynthesis"
node index.js "what is a neural network"
node index.js "inflation"
```

The first run downloads the model (Llama 3.2 1B Q4). Subsequent runs are instant and fully offline.
